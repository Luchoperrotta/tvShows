<%= form_with(model: episode) do |form| %>
  <% if episode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(episode.errors.count, "error") %> prohibited this episode from being saved:</h2>

      <ul>
        <% episode.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :numer %>
    <%= form.number_field :numer %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :emmited_at %>
    <%= form.datetime_select :emmited_at %>
  </div>

  <div class="field">
    <%= form.label :minute %>
    <%= form.number_field :minute %>
  </div>

  <div class="field">
    <%= form.label :season_id %>
    <%= form.collection_select(:season_id, Season.all,:id, :number) %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
